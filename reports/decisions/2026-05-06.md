# Decisions — Wed 2026-05-06 (Phase 1 Day 6)

## Morning incident: engines silently dead, root-caused & fixed

### Timeline
- [Yesterday 18:15 IST] Claude restarted backend off-hours for bot-token-swap → backend left running overnight (violated `feedback_no_off_hours_service_start.md`)
- [Today 03:30 UTC = 09:00 IST] cron `start-if-trading-day.sh` fired, did `systemctl start honeydaalu-backend` — **NO-OP** because service was already running. AutoStart logic in main.py never re-fired.
- [09:00 - 09:59 IST] Both paper engines silently dead. Backend running but no engines. Morning ping at 09:20 IST reported `🚨 Intraday engine NOT running` — owner did not see (notification gap).
- [09:59 IST] Owner pinged "Luckyhoney didn't send any morning notification yet"
- [09:59 IST] Claude diagnosed, manually restarted backend → engines came up
- [10:00 IST] Discovered swing had OLD strategies (play5+play6 still loaded from yesterday's state) — separate bug
- [10:05 IST] Pushed cron fix `start` → `restart` (prevents recurrence)
- [10:06 IST] Pushed main.py fix (added swing_paper_trader to AutoStart stale-engines list)
- [10:06 IST] Backend restarted via deploy → swing now correctly loaded with [play2, play1]

### Two distinct root causes, both fixed

1. **Cron `start` was no-op when service running.** Fixed by changing to `systemctl restart` in `deploy/start-if-trading-day.sh` (commit 480c735). From tomorrow's 09:00 IST cron onward, restart is idempotent — fires AutoStart cleanly regardless of prior state.

2. **AutoStart cleanup missed swing_paper_trader.** Engine's `_load_state` auto-resumed with yesterday's strategy list. AutoStart cleanup loop didn't include swing → couldn't stop it → `start()` returned "already running" with old strategies. Fixed by adding to `_stale_engines_all` list (commit 5a89bfa).

### Current state (10:08 IST)
- Backend service: active (started 10:06:03 IST)
- Swing engine: running, strategies = [play2_triple_ma, play1_ema_crossover] ✓
- Intraday engine: running, strategy = [play1_ema_crossover] ✓
- Capital: ₹75k swing / ₹50k intraday
- 0 active trades, scans=0 fresh
- Realism rules from yesterday active: per-position cap, time-stop @14:30, breakeven trail @+0.5%

## Notification gap — closed for diagnostics

- Fresh bot @Luckyhoneymbot delivered today's 09:20 IST morning ping (server log: `[ok] sent Telegram`)
- Owner did NOT receive on phone — confirms 100% phone-side, not bot/server
- Per owner's Option 2 choice 2026-05-05: accept morning gap, rely on EOD + manual prompts
- No further notification debugging. Architecture stays single-channel @Luckyhoneymbot.

## Self-inflicted incident tally (rolling)

- 2026-05-04: mid-market push wiped intraday state
- 2026-05-05 → 2026-05-06: off-hours restart left backend running overnight, cron no-op killed engines
- **Total: 2 self-inflicted P1 incidents in 5 trading days.** Discipline calibration ongoing.

## Decisions executed (Claude, autonomous after owner authorization)

### 1. Cron `start` → `restart` permanent fix (commit 480c735)
- File: `deploy/start-if-trading-day.sh`
- Idempotent: starts if stopped, restarts if running. Always re-fires AutoStart logic.
- Effective: tomorrow's 09:00 IST cron and all subsequent.
- Rollback: `git revert 480c735` (would re-introduce no-op gotcha).

### 2. swing_paper_trader added to AutoStart stale-engines list (commit 5a89bfa)
- File: `backend/main.py`
- Closes config-stickiness bug where swing booted with stale state's strategy list.
- Mid-market push, but engines had 0 active trades. Restart took 3 sec.
- Rollback: `git revert 5a89bfa` (would reintroduce stickiness).

### 3. Manual backend restart at 09:59 IST
- One-time recovery action to bring engines up after morning's silent failure.
- Without permanent cron fix, recurrence likely. Permanent fix shipped same session.

## Open priorities for rest of Day 6

- [Now → 15:30 IST] **Path C backtest rebuild** — start the multi-day work
  - 12+ months NSE data
  - Walk-forward 70/30 train/test split
  - Next-bar-open execution model (no same-bar-close look-ahead)
  - Full Indian retail cost model (brokerage + STT + GST + DP + stamp + SEBI)
  - Multi-regime testing
  - Sample size enforcement: n≥100 per strategy
- Target completion: Mon 18 May 2026 (Day 14)
- Outcome: valid baseline for Gate 1 decision Tue 26 May 2026

## Notes

- Status quo: 7 commits today already (cron + swing fix + 5 deploys verifying). Discipline check: every push today had a clear reason; no scope creep.
- Tomorrow's 09:00 IST AutoStart will be the first test of the cron `restart` fix. If it works, the fragility is genuinely closed. If not, deeper investigation needed.
